backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

collections:
  - name: "posts"
    label: "Posts"
    folder: "posts"
    create: true
    slug: "{{ slug }}"
    fields:
      - {
        label: "Title",
        name: "title",
        widget: "string"
      }
      - {
        label: "Publish Date",
        name: "date",
        widget: "datetime"
      }
      - {
        label: "Post Body",
        name: "body",
        widget: "markdown"
      }
  - label: "Services"
    name: "services"
    folder: "services"
    fields:
        - { label: "Title", name: "layout", widget: "hidden", default: "/layouts/homepage.njk" }
        - { label: "Icon", name: "faIcon", widget: "string" }
        - { label: "Description", name: "description", widget: "text" }
        - { label: "Service Body", name: "Service", widget: "markdown" }
  - label: "Site Settings"
    name: "settings"
    folder: "_data"
    file: "_data/settings.json"
    fields:
        - { label: "Site Name", name: "siteName", widget: "string" }
        - { label: "Logo", name: "logo", widget: "image", required: false }
        - { label: "About Text", name: "about", widget: "text" }

media_folder: "_site/assets/img"
public_folder: "img"